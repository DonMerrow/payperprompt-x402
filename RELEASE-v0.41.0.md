# PayPerPrompt x402 v0.41.0

## Public repository edition

- Removes the original payer address from configurable browser and launcher
  runtime paths.
- Adds guided setup for a disposable payer public address, distinct merchant
  public address, and durable USDC policy limits.
- Loads wallet allowlist configuration through a read-only public endpoint.
- Keeps every private key, seed phrase, password, and signature inside the
  user's recognized browser wallet.
- Excludes `.env.local` from Git and public release archives.
- Replaces the historical development README with a focused installation,
  operation, architecture, evidence, SDK, safety, and hosting guide.
- Adds wallet configuration, public repository, contribution, security, and
  judge-availability documentation.
- Adds a static GitHub Pages proof page so the submission remains reviewable
  while the laptop-hosted interactive demo is offline.

The representative Base Sepolia proof remains unchanged. No payment was signed
or sent to produce this release.
