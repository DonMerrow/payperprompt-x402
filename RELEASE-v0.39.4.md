# PayPerPrompt x402 v0.39.4

## Guided paid AI workspace

v0.39.4 makes the full work-type catalog understandable and testable from the
browser:

- Every type includes a distinct realistic request, not a recycled product-copy
  prompt.
- Guidance explains the input that produces a useful result.
- Expected route and pricing are visible before preparation.
- Selecting another type updates a known suggestion automatically.
- A custom user request is never silently overwritten.
- **Use suggested request** provides an explicit way to load the example.
- Planning prepares and validates the hidden deliverable without opening
  MetaMask or spending USDC.

The prepared-work commitment, semantic gates, exact x402 charge, trusted-wallet
allowlist, one-time settlement release, live Base Sepolia proof, and independent
Rust verification remain unchanged.
